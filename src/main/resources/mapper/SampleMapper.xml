<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gips.taskapp.mapper.SampleMapper">

    <select id="getSampleList" resultType="com.gips.taskapp.dao.Sample">
        SELECT
         login_id,
         user_name,
         password,
         role_id 
        FROM users
    </select>

</mapper>